import React from 'react';
import SearchBar from './SearchBar';

export default React.createClass({
  render() {
    return (
      <div>
        <h1>Playlist page</h1>
        <SearchBar />
      </div>
    )
  }
});
